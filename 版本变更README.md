# DfoGmTool_260716 版本变更说明

本文档记录本次会话针对 `DfoGmTool_260716` 的功能修正、服务端规则对齐和界面增强，便于提交到 DfoGmTool 的合并请求中作为变更说明。

## 核心目标

- 修正 GM 工具中等级、经验、SP/TP、转职、任务、称号簿、发放物品、背包配置等功能与当前服务端实现不一致的问题。
- 将物品、装扮、装备、技能、称号簿、任务等关键逻辑尽量对齐服务端与当前 PVF 数据，而不是依赖旧工具的写死规则。
- 增强 GM 工具在发放和修改物品时的可用性，降低写入错误数据导致角色异常、背包异常或客户端显示不一致的风险。

## 等级、经验、SP/TP、转职

- 修正角色等级和经验写入逻辑，使等级与经验阈值保持匹配。
- 修正 SP/TP 调整逻辑：
  - 调整后刷新技能点状态。
  - 调整附加点后输入框归零，避免误操作。
  - 传入负数时不会把附加 SP/TP 扣成负数。
  - 新增剩余 SP/TP 归零功能，并在前后端都做合法性校验。
- 修正转职/觉醒写入逻辑：
  - 转职数据按服务端格式写入。
  - 修改转职后自动初始化技能列表。
  - 清理旧职业残留技能。
  - 重置技能点到当前等级默认状态。

## 发放物品与背包分类

- 发放物品时严格按照 PVF 解析出的物品类型选择发放路径。
- PVF 不存在的物品禁止发放。
- PVF 存在但类型不明确的物品，允许用户手动指定发放类型。
- 修正神器克隆装扮等装扮类物品被错误发放到装备栏的问题。
- 五种晶块及相关晶块道具改为按账号晶块数据累加，不再直接插入背包。
- 搜索结果中需要配置的装备、装扮、期限道具不再直接行内发放，改为在下方配置卡片确认发放。

## 装备发放与配置

- 发放装备时支持配置：
  - 装备品级：随机品级、100% 最上级。
  - 强化/增幅，最高 31。
  - 武器锻造，最高 8。
  - 红字属性，按 PVF `[option data]` 顺序处理：
    - 1 = 体力
    - 2 = 精神
    - 3 = 力量
    - 4 = 智力
- 强化/增幅限制对齐服务端：
  - 仅武器、防具、首饰、特殊装备可强化/增幅。
  - 仅 55 级及以上、紫色及以上装备可添加红字。
  - 有红字属性时按增幅处理，无红字属性时按强化处理。
- 背包装备页新增配置能力：
  - 只对可配置装备显示“配置”按钮。
  - 称号、副武器等无配置类型不显示配置入口。
  - 修改装备配置时只改已知装备字段，不破坏附魔、宝珠和其他未知 `extra_json` 数据。

## 装扮发放与配置

- 装扮发放根据选定角色职业过滤可用装扮。
- 装扮配置支持不同部位的可选属性。
- 上衣技能列表改为使用当前 PVF 的 `skill/abilitydatas.dat`：
  - 通过 `.equ` 中的 `[ability case index]` 定位技能列表。
  - 上衣技能回传值按实际下拉顺序从 `0` 开始编号。
  - 技能中文名仍通过当前 PVF 的技能文件解析。
- 下装及其他部位继续使用 `.equ` 中的 `[avatar select ability]`。
- 非技能属性中文名改为使用当前 PVF 的 `etc/avatarabilitystringtable.etc`。
- 删除旧的装扮技能猜测/兜底列表，避免不同微调 PVF 中技能列表不一致。
- 背包装扮页新增配置能力：
  - 只对可解析配置的装扮显示“配置”按钮。
  - 只修改已知 avatar selector 相关数据。
  - 不破坏其他未知 `extra_json` 字段。
- 发放/配置界面去掉“模板品级”只读展示项，保留真正需要操作的配置项。

## PVF 动态读取与初始化

- PVF 初始化/切换时会清理旧缓存并预热当前 PVF 数据。
- 技能列表不再依赖固定职业技能文件名，而是读取当前 PVF 的：
  - `skill/skilllist.lst`
  - 由 `skilllist.lst` 引用的各职业技能表，例如 `skill/atgunnerskill.lst`、`skill/thiefskill.lst` 等。
- 装扮能力相关数据读取当前 PVF 的：
  - `skill/abilitydatas.dat`
  - `etc/avatarabilitystringtable.etc`
- 确认没有从 `PvfUtility\提取内容` 读取运行时数据。

## 任务与称号簿

- 保留当前版本的一键称号簿实现。
- 新增一键取消称号簿任务功能：
  - 将当前成就页任务标记为未完成。
- 主线任务区域新增一键完成当前等级主线。
- 任务与称号簿相关操作参考服务端任务实现，避免写入错误任务状态。

## 仓库、地图难度、账号备份

- 同步一键满级仓库功能：
  - 角色个人仓库满级放在角色属性页。
  - 账号金库满级放在账号页面。
- 角色属性页新增一键开启地图难度。
- 增加账号备份功能，并结合当前服务端数据库差异处理。

## 角色删除与复制

- 角色属性页新增彻底删除角色功能：
  - 需要二次确认。
  - 前端确认框要求输入“删除角色”。
- 修正删除角色后的 `seed_character_id` 兜底逻辑：
  - 优先选择同账号剩余角色。
  - 没有同账号角色时选择其他有效角色。
  - 如果没有有效角色则回退到模板角色。
  - 避免删除角色后客户端选择角色界面因种子角色无效而异常。
- 新增复制角色功能：
  - 支持复制到当前账号或其他账号。
  - 支持新建目标账号，密码以 MD5 存储。
  - 校验目标账号角色数量上限。
  - 校验新角色名重复。
  - 支持按类型选择复制角色相关数据，例如背包、装备、装扮、宠物、个人仓库等。
  - 不复制账号级资源，例如账号金库、晶块、点券等。
  - 复制时处理主键冲突，避免覆盖原角色数据。

## 删除与刷新体验

- 优化背包删除后数据同步，减少删除后需要进游戏手动排序的问题。
- 背包和任务相关操作尽量走服务端同源逻辑，减少客户端刷新状态不一致。

## 自测与发布

已执行并通过：

- `dotnet build`
- 发布版 `DfoGmTool.exe --selftest-item-grant-options`
- 发布版 `DfoGmTool.exe --selftest-character-mutations`

发布目录：

```text
F:\Program Files\DNF_86jp\Codes\DfoGmTool_260716\dist\win-x64
```

## 主要修改范围

- `Services/`
- `ServerCore/Game/Inventory/`
- `ServerCore/Game/Skills/`
- `ServerCore/GameWorld/`
- `PvfLib/Models/EquipmentFile.cs`
- `SelfTests/`
- `wwwroot/js/give.js`
- `wwwroot/js/inventory.js`

## 合并请求说明建议

建议合并请求标题：

```text
修正 GM 工具角色修改、物品发放、装扮配置与 PVF 动态读取逻辑
```

建议合并请求摘要：

```text
本次变更将 GM 工具的角色成长、转职、技能点、任务、称号簿、物品发放、装备/装扮配置等逻辑进一步对齐服务端与当前 PVF 数据。重点修复等级经验不匹配、转职后技能未初始化、装扮技能列表不准确、装备/装扮 extra_json 配置不完整、物品发放分类错误、角色删除后种子角色状态异常等问题。

装扮能力和技能列表改为在 PVF 初始化时读取当前 PVF 的 skilllist、abilitydatas.dat 和 avatarabilitystringtable.etc，以适配不同微调 PVF。
```
